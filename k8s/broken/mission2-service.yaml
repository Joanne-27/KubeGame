# Mission 2 — The Door With the Wrong Label
# FLAW: selector uses "gaurd" (typo) — produces zero endpoints
apiVersion: v1
kind: Service
metadata:
  name: greeting-service
  namespace: cluster-heist
spec:
  type: ClusterIP
  selector:
    app: gaurd          # ← BROKEN: typo, should be "guard"
    component: greeting
  ports:
    - port: 80
      targetPort: 80
